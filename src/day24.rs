pub fn solve() -> (usize, usize) {
    (42,42)
}

struct ALU {
    w: isize,
    x: isize,
    y: isize,
    z: isize,
    program: Vec<String>,

}

impl ALU {
    
    pub fn execute(input: Vec<isize>) -> isize {
        
    
    }
}
